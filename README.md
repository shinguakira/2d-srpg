# FE 聖魔の光石風 2D SRPG

『ファイアーエムブレム 聖魔の光石』(GBA) の戦闘システムと会話パートを再現した
2D シミュレーション RPG。TypeScript + Canvas 2D のみ。ゲームエンジンもフレームワークも
状態管理ライブラリも使っていない。

```bash
npm install
npm run dev      # http://localhost:5173
```

### 開発コマンド

| コマンド | 内容 |
| --- | --- |
| `npm run build` | 型チェック + 本番ビルド |
| `npm run lint` / `lint:fix` | oxlint（`--fix` で自動修正） |
| `npm run format` / `format:check` | oxfmt |
| `npm run knip` | 未使用のファイル・エクスポート・依存の検出 |
| `npm run check` | tsc → oxlint → oxfmt --check → knip をまとめて実行 |

設定は `.oxlintrc.json` / `.oxfmtrc.json` / `knip.json`。
oxfmt は Markdown を対象外にしている（表の桁揃えが全角文字を 1 桁と数えて崩れるため）。

## 操作

| キー | 動作 |
| --- | --- |
| ↑↓←→ / WASD / マウス移動 | カーソル |
| Z / Enter / 左クリック | 決定（ユニット選択 → 移動先 → コマンド → 対象）／会話送り |
| X / Esc / 右クリック | キャンセル（移動前の位置に戻る）／会話の早送り |
| R | カーソルの下のユニットの詳細（左右でページ） |
| E | 自軍フェイズを終了 |
| Shift+R | リスタート |
| 指でのタップ | マスを押すと決定。押したまま滑らせるとカーソルだけ動く |

章の目標は章ごと（第1・2章は玉座の制圧、第3章は敵の全滅）。敗北条件はロード
（シゲル）の死亡。**スマホでも遊べる** —— 縦持ちなら画面を 90 度回して全面に
広げ、盤の左右の柱に決定・戻る・メニュー・詳細のボタンが出る。

## 仕様書

**計算式・システムの仕様は `specs/systems/` にある。**
コードから書き起こしたもので、こちらが正。

| | |
| --- | --- |
| [combat.md](specs/systems/combat.md) | 戦闘計算（命中・ダメージ・追撃・特効・経験値） |
| [units.md](specs/systems/units.md) | 能力値・クラス・武器レベル・クラスチェンジ |
| [support.md](specs/systems/support.md) | 支援システム（属性表・3マス判定・友好度） |
| [map-and-turns.md](specs/systems/map-and-turns.md) | 地形・移動・ターン進行・敵 AI |
| [presentation.md](specs/systems/presentation.md) | 描画・カメラ・スプライト |

`specs/story/` はアマギ島を舞台にした25章のキャンペーン設計。**実装の説明ではなく、
目標**。実際に入っているのは第1〜3章と、シゲルとアキラの絵だけ。

## 実装済みの要素

**戦闘計算** —— GBA 系 FE の式そのまま。**命中判定は 2RN**、武器三すくみ（威力±1 /
命中±15）、特効は武器威力×3、必殺は×3、追撃は攻速差4以上、S ランクで命中・必殺+5、
経験値はクラス補正込みの聖魔式。救出中は技と速さが半分。

**ユニット** —— 能力10種、武器レベル E〜S と熟練度、聖魔式の**2択クラスチェンジ**、
援護（Aid）、支援（属性の組み合わせ／3マス以内／合計5ランクまで）。

**マップ** —— 地形17種と守備・回避・毎ターン回復、玉座の魔防+5、飛行は砦・門・
玉座しか地形効果を受けない。移動タイプ別コスト、ダイクストラの移動範囲、経路矢印。
**カメラ**は盤面が画面より大きくてよく、注目点を追ってスクロールする。

**コマンド** —— 制圧 / 訪問 / 宝箱 / 扉 / 武器屋 / 闘技場 / 攻撃（**武器を選んでから
対象**）/ 杖（回復・リザーブ・スリープ等）/ 救出・降ろす・引き取る / 輸送隊 /
トレード / 会話 / 支援 / 道具 / 待機。騎馬と飛行は行動後に**再移動（カント）**。

**状態異常** —— 睡眠・沈黙・狂戦・毒。杖と毒の牙が撒き、リザーブが解く。

**増援** —— 章の定義に `{turn, at, seed}` を並べると、そのターンの頭に湧く。

**章の外** —— タイトル（中断から再開 / はじめから / つづきから / ガイド）、
ワールドマップ（章・行商・影の塔・魔物の群れ）、準備画面（ユニット / アイテム /
支援 / マップ確認 / セーブ / 出撃）、セーブと**中断**。

**画面** —— 常設の HUD は置かない（GBA FE もそう）。地形ウィンドウ、ユニット
ウィンドウ、戦闘予測、フェイズ頭の目標ウィンドウが出入りするだけ。オプションは
10項目（戦闘アニメ3段階・ゲーム速度・文字送り・各ウィンドウ・目標表示・オート
カーソル・オートターンエンド・ウィンドウカラー）。

**敵 AI** —— 攻撃可能な全マス × 全対象を評価（期待ダメージ・撃破可否・反撃リスク・
地形）して最善手を選ぶ。`aggressive` / `guard` / `boss`。狂戦は敵味方を見ない。

**魔物** —— 屍兵・バエル・モーグル・ガーゴイル。光魔法と神聖武器が特効。

## 会話パート

FE 風のテキストボックス（立ち絵 + 名前ウィンドウ + タイプライター表示）。

| 種類 | 発生条件 |
| --- | --- |
| オープニング | ゲーム開始時 |
| 支援会話 C/B/A | 隣接した支援ペアで「支援」コマンド |
| 説得 | シゲルが傭兵ロウに隣接して「会話」→ **仲間になる** |
| 戦闘前会話 | シゲルがボス・ヴァルガに隣接して「会話」→ ヴァルガの守備 −2 |
| 死亡時のセリフ | ユニットが倒れたとき |
| エンディング / 敗北 | 決着時 |

## 構成

```
src/
  main.ts                 入力・章の外の画面遷移・ループ・開発用シーン
  types.ts
  core/rng.ts             xorshift32 + 2RN 命中判定
  core/grid.ts            移動範囲（ダイクストラ）・経路・射程
  battle/combat.ts        戦闘計算・杖・状態異常
  battle/support.ts       支援システム
  battle/battleScene.ts   戦闘アニメーション演出
  story/dialogue.ts       会話パートのエンジン
  story/script.ts         台本（オープニング / 支援 C・B・A / 説得 / 死亡 / エンディング）
  game/game.ts            章の中のステートマシン
  game/campaign.ts        章をまたぐもの（部隊・所持金・輸送隊・オプション・セーブ）
  game/options.ts         オプションの定義
  game/suspend.ts         中断と再開
  game/ai.ts              敵 AI
  render/layout.ts        タイルサイズ・表示窓・カメラ
  render/viewport.ts      画面に合わせる（拡大・縦持ちの回転・座標の逆変換）
  render/touch.ts         指で押すボタン
  render/mapRender.ts     マップ・ウィンドウ・全画面パネル
  render/screens.ts       タイトル・ワールドマップ・準備・行商・ガイド
  render/sprites.ts       スプライトと立ち絵
  data/                   地形・武器・クラス・章・ユニット定義・ガイド
  assets/sprites/         シゲルとアキラのアニメーションシート
```

## 開発用 URL パラメータ

スクリーンショットや演出確認のために、起動時の状態を作れる。

```
?touch=1             PC でも指向けの UI を出す
?dev=move            ユニット選択 + 移動範囲
?dev=menu            移動後の行動メニュー
?dev=target          戦闘予測パネル
?dev=battle          戦闘アニメーション
?dev=levelup         レベルアップ演出
?dev=opening         オープニングの会話
?dev=talk            説得の会話
?dev=support         支援会話
&adv=1.5             指定秒数だけ進めた状態で停止
```

（`?dev=` を付けるとオープニングの会話はスキップされる）

## 閲覧ページ

書いたものを、遊ばずに全部見るための三つ。

### `/story.html` —— 通しで読む

**台本を頭から終わりまで読むページ。** dev サーバでだけ出る（vite の入口は
`index.html` だけなので production の束には入らない）。`src/storyPage.ts`。

左に章の目次、右に本文。話者は行頭の桁に色分けで並び、地の文は落として斜体。
検索は節ごと絞り込んで一致箇所に印を付けるので、**言い回しがどの章に散って
いるかがそのまま見える**（「当番」で引くと第23章と終章の六本だけが残る）。

読むついでに粗も出す ——

- 顔グラの無い話者（`who` が `PORTRAIT_UNITS` に無い）
- 吹き出しが出せない `**` が本文に混ざっている行
- 一息で読ませるには長い行（64 字以上）

`?q=灰` で検索した状態から開く。「id を出す」を入れると話者 id と台本 id が並ぶ。

### `?dev=story` / `?dev=maps` —— canvas のほう

`src/render/devBrowse.ts`。読むのには向かないが、**本編とまったく同じ演出で
一本再生できる**のはこちら。盤の閲覧も同じ画面群にある。

```
?dev=story           台本一覧 —— ↑↓ で選んで Z で再生。?s=132 でその行から開く
?dev=maps            盤の閲覧 —— 章の盤を一枚に収めて、出撃・敵・ボス・増援・
                     村・宝・味方NPC の位置と、章ごとの内訳を出す。?ch=24 で直接
```

| 画面 | ↑↓ | ←→ | Z | X |
| --- | --- | --- | --- | --- |
| 台本一覧 | 一行ずつ | 十行ずつ | 本編と同じ演出で再生 | 盤の閲覧へ |
| 再生中 | — | — | 送る | 閉じる |
| 盤の閲覧 | 盤を切り替え | 盤を切り替え | 全部の盤を点検 | 台本一覧へ |
| 盤の点検 | — | — | 戻る | 戻る |

盤には本編十章のあとに影の塔と魔物の群れが続く。**点検**は置き間違いを名指しする
—— 通れない地形の上のユニットや湧き口、地形が村でない村、玉座でない制圧目標、
盤に出てこないのに「守れ」と言われている者、名簿にいない強制出撃。増援の湧き口は
`Game.freeNear` が近くの床へ逃がしてしまうので、遊んでいる限り気づけない。

## 調整するなら

- 自軍の能力・属性・持ち物: `src/data/roster.ts`
- 章のマップ・敵・村・宝箱・店・増援・闘技場: `src/data/chapters/ch1.ts` ほか
- 武器の威力・命中・必殺・射程・特効・杖の効き方: `src/data/weapons.ts`
- クラスの武器適性・クラスチェンジ先・スキル: `src/data/classes.ts`
- 地形: `src/data/terrain.ts`
- オプションの項目: `src/game/options.ts`
- 台本: `src/story/script.ts`
- AI の評価関数: `src/game/ai.ts` の `evaluate()`
- タイルサイズ・表示窓・カメラの追従: `src/render/layout.ts`

## 絵づくり

**シゲルとアキラだけが画像**。`src/assets/sprites/shigeru-sheet.png`（46フレーム /
7クリップ）と `akira-sheet.png`（38フレーム / 6クリップ、歩行なし）を
`render/sprites.ts` が切り出して描いている。PixelLab に登録した
キャラクターから生成しているので、全フレームが同じ絵になっている。

それ以外のキャラクターは PoC から引き継いだコード描画のまま。**新しく描き足すのは
禁止**。理由と、絵を増やすときに何をしてよいかは [AGENTS.md](AGENTS.md) にある。

引き継いだコード描画は GBA 系 FE の実画面を見て次の特徴を取り込んでいる。

- **会話**: 枠なしの立ち絵が画面下端で切れ、クリーム色の吹き出し（茶色の縁＋話者へ伸びるしっぽ）に濃色のテキスト
- **顔グラ**: 髪のシルエットを顔より大きく取り、毛先を尖らせる。目はアーモンド型で虹彩をまぶたで切り、
  太い上まつげとハイライトを入れる
- **UI**: 青灰グラデーションのウィンドウ＋明色の細枠。ラベルは金、数値は白の縁取り文字
- **コマンドメニュー**: 上下に木のバーが付いた巻物風。選択カーソルは指差しの手
- **戦闘画面**: 上隅に金枠の名前プレート、隅に 命中/威力/必殺 の小箱、HP は目盛り式ゲージ
- **マップスプライト**: 2.5 頭身、暗色のアウトライン、受け光、武器を持つ腕

## 参考にした資料

- [支援効果 ‐ ファイアーエムブレム 聖魔の光石 | RRPG](https://rrpg.jp/fe_seima/support.html)
- [Fire Emblem The Sacred Stones - Support system | Fire Emblem WoD](https://www.fireemblemwod.com/fe8/ENG_apoyo.htm)
- [Supports / calculation | Serenes Forest](https://serenesforest.net/the-sacred-stones/characters/supports/calculation/)
- [3すくみ - ファイアーエムブレム用語辞典](https://w.atwiki.jp/fedic/pages/82.html)
- [クラスチェンジ ‐ ファイアーエムブレム 聖魔の光石 | RRPG](https://rrpg.jp/fe_seima/cc.html)
- [Chapter guide | Serenes Forest](https://serenesforest.net/the-sacred-stones/general/chapter-guide/) — 各章の出撃数・敵数・援軍の実データ
- 絵づくりの参考（実画面の確認用）: [Fire Emblem: The Sacred Stones - Fire Emblem Wiki](https://fireemblemwiki.org/wiki/Fire_Emblem:_The_Sacred_Stones) ／ [Eirika/Gallery](https://fireemblemwiki.org/wiki/Eirika/Gallery)
